<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.apropal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CadastrarAgricultor"></activity>
        <activity android:name=".CadastrarTecnico" />
        <activity android:name=".CadastrarInsumo" />
        <activity android:name=".ListaPresuncao" />
        <activity android:name=".ListaInsumo" />
        <activity android:name=".ListaAgricultor" />
        <activity android:name=".ListaFazenda" />
        <activity android:name=".ListaTec" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>